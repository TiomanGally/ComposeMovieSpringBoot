apiVersion: apps/v1
kind: Deployment
metadata:
  name: movie
  namespace: test
  labels:
    app: movie
spec:
  replicas: 2
  selector:
    matchLabels:
      app: movie
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: movie
    spec:
      containers:
        - image: milchkarton/compose:movie-v23
          imagePullPolicy: IfNotPresent
          name: movie-v18
          ports:
            - containerPort: 8080
              protocol: TCP
          envFrom:
            - secretRef:
                name: omdb-api-key-secret
            - configMapRef:
                name: movie-configmap
